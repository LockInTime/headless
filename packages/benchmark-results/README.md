# Benchmark results

`results.json` is generated by `apps/headless/benchmark.sh`. It contains every
raw sample and the median of each metric, plus the repeat count, container
platform, generation time, generator, and benchmark method. Do not edit the
result document by hand.

Regenerate it from the repository root with:

```sh
./apps/headless/benchmark.sh 5
```
